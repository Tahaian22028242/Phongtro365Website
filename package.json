{
  "name": "phongtro365",
  "private": true,
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "build:api": "npm run build --workspace=api || true",
    "build:client": "npm run build --workspace=client",
    "build": "npm run build:client",
    "dev": "node api/index.js",
    "start": "node api/index.js"
  },
  "dependencies": {
    "@google/genai": "^2.17.1"
  }
}
